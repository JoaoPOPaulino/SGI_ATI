{
  "name": "sgi-ati",
  "private": true,
  "scripts": {
    "postinstall": "cd docker/backend && npm install",
    "build": "cd docker/backend && npm install",
    "start": "cd docker/backend && npx tsx src/index.ts"
  },
  "engines": {
    "node": ">=18"
  }
}
